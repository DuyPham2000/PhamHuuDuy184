@model IEnumerable<PhamHuuDuy184.Models.PersonPHD184>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PersonName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PersonName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Personld">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.Personld">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Personld">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
